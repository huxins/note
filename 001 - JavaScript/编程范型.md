# 编程范型

编程范型或编程范式，是指软件工程中的一类典型的编程风格。

## 一、函数式编程

### 惰性函数

惰性函数表示函数执行的分支，只会在函数第一次调用的时候执行，在第一次调用过程中，该函数会被覆盖为另一个按照合适方式执行的函数。

例如，返回首次调用时的 `Date` 对象。

```javascript
var foo = function() {
    var t = new Date();
    foo = function() {
        return t;
    };
    return foo();
};
```

